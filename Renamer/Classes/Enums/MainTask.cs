namespace Renamer
{
    /// <summary>
    /// The tasks that can be started from the main screen
    /// </summary>
    public enum MainTask : int
    {
        OpenDirectory,
        DownloadData,
        CreateRelations,
        Rename
    };
}